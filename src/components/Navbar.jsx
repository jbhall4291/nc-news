import React from 'react';
import "../styling/Navbar.css"

const Navbar = () => {
    return (
        <section className="navbar">
            <h2 className="navbar__h2">this is the navbar placeholder</h2>
        </section>
    );
};

export default Navbar;